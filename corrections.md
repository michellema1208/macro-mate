# Corrections Log

Running record of decisions on proposals from the spec reconciliation agent (`spec-agent.md`). Read by the agent at the start of every run so its proposals improve over time.

Format per entry:

```markdown
## YYYY-MM-DD
- Proposed: [what the agent suggested]
- Decision: accepted / rejected / edited
- Note: [why, if rejected or edited — this is what sharpens future runs]
```

Entries below, most recent first.
